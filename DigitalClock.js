/* Write a fuction that takes the number of 
seconds and returns the digit format clock time as a string. Time should be counted from 00:00:00.
*/

console.log(digitalClock(5025));
console.log(digitalClock(61201));
console.log(digitalClock(87000));
